{
  "name": "plotter-utils",
  "version": "1.0.0",
  "private": true,
  "description": "Pure-TS plotting utilities: SVG <-> G-code converters and the pen-up path optimizer.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "tsc && node tests/run_all.js"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
