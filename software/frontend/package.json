{
  "name": "plotter-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Static Svelte frontend for the plotter: submit drawings, queue, gallery, and the admin/tuning panel. Served by the Pi backend (or any static host).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "svelte": "^5.20.0",
    "typescript": "^5.8.0",
    "vite": "^6.2.0"
  }
}
